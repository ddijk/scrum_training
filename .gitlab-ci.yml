image: harmpauw/maven-letsencrypt

stages:
  - test
  
test:  
  script:
    - mvn surefire-report:report
    - mvn site -DgenerateReports=false
  artifacts:
    when: always
    name: Unit test report
    paths:
    - target/site/
